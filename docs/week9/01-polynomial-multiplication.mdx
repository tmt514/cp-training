---
sidebar_position: 2
title: 'Polynomial Multiplication'
exercises: []
---

# {frontMatter.title}

The previously discussed problems can all be solved via multiplications of polynomials.

Consider two polynomials $A(x)=a_0+a_1x+a_2x^2+\cdots+a_{n-1}x^{n-1}$
and $B(x)=b_0+b_1x+b_2x^2+\cdots+b_{n-1}x^{n-1}$.
The goal is to compute their product $C(x)=A(x)B(x)$.

## Diversion: Inspired by Gauss

How to compute $(a+bi)(c+di)$ using as few multiplications as possible?

## Karatsuba's Algorithm 

(easy)

## Point-Value Representation

(easy)

## Discrete Fourier Transform (DFT) 

Given the coefficient of $A(x)$: $(a_0, a_1, \ldots, a_{n-1})$, output $A(\omega_n^0), A(\omega_n^1), \ldots, A(\omega_n^{n-1})$.

## Fast Fourier Transform [Gauss 1805, Cooley-Tukey 1965]

(easy)

## Vandermonde Matrix 

(easy)

## Inverse Vandermonde Matrix

(easy)

## Inverse FFT

(easy)
